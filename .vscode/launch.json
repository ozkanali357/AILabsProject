{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run RSA CLI",
            "type": "python",
            "request": "launch",
            "module": "projectcode.rsa.clinterface",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}